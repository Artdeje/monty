#include "monty.h"

/**
 * m_queue - sets the format of the data to queue
 * @stack: pointer to the beginning of the stack
 * @line_number: line number of operation
 *
 * Return: nothing
 */

void m_queue(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;

	var.queue = QUEUE;
}
