#include "monty.h"

/**
 * m_stack - sets format of data to stack
 * @stack: pointer to the beginning of the stack
 * @line_number: line number of operation
 *
 * Return: nothing
 */

void m_stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;

	var.queue = STACK;
}
